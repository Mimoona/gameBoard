package org.example;

public class Main {
    public static void main(String[] arg) {
        PlayerCharacter playerFigure = new PlayerCharacter();
        System.out.println("value of X " + playerFigure.getX() + "\n" +"value of Y " + playerFigure.getY());
    }
}
